import type { NextConfig } from "next";

// Temporarily disabling PWA to resolve build and client-side errors.
// The core Next.js configuration remains.

const nextConfig: NextConfig = {
  /* You can add Next.js specific config options here if needed */
};

export default nextConfig;
